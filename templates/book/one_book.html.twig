{% extends 'base.html.twig' %}

{% block title %}{{book.title}}{% endblock %}

{% block body %}
    <div class="alert alert-primary" role="alert">
  <h4 class="alert-heading">{{book.title}}</h4>
  <p>{{book.description}}</p>
  <hr>
  <p class="mb-0">{{book.pages}} pages - Par {{book.author}}</p>
</div>
<a class="btn btn-warning" type="button" href="{{path('home')}}">Retourner à la page principale</a>
<a class="btn btn-warning" type="button" href="{{path('editBook', {id:book.id})}}">Modifier ce livre</a>

{% endblock %}
